import 'package:flutter/material.dart';

class SetttingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Here is the settings stuffs."),
      ),
    );
  }
}
